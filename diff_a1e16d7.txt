diff --git a/frontend/styles.css b/frontend/styles.css
index 4fb8cf2..22fea38 100644
--- a/frontend/styles.css
+++ b/frontend/styles.css
@@ -16,6 +16,36 @@
     --transition-smooth: 0.4s cubic-bezier(0.4, 0, 0.2, 1);
 }
 
+/* Status Indicators */
+.status-badge {
+    display: inline-block;
+    padding: 0.25rem 0.75rem;
+    border-radius: 9999px;
+    font-size: 0.75rem;
+    font-weight: 600;
+    text-transform: uppercase;
+    letter-spacing: 0.025em;
+}
+
+.status-good {
+    background-color: rgba(16, 185, 129, 0.1);
+    color: #059669;
+}
+
+.status-warning {
+    background-color: rgba(245, 158, 11, 0.1);
+    color: #d97706;
+}
+
+.status-critical {
+    background-color: rgba(239, 68, 68, 0.1);
+    color: #dc2626;
+}
+
+.insight-card .status-badge {
+    margin-bottom: 0.75rem;
+}
+
 .hidden {
     display: none !important;
 }
@@ -30,10 +60,9 @@
     margin-left: 0;
 }
 
-/* Dashboard layout: main content is offset by the collapsed sidebar width.
-   Sidebar expands (overlays) rightward on hover — no reflow. */
+/* Dashboard layout: Main content flexes adjacently to the sidebar. */
 .layout-dashboard .main-content {
-    margin-left: 64px;
+    margin-left: 0;
     height: 100vh;
     overflow-y: auto;
 }
@@ -101,10 +130,9 @@ body {
     min-height: 100vh;
 }
 
-/* Sidebar Container — icons-only by default, expands on hover */
+/* Sidebar Container — flex item, pushes content on expand */
 .sidebar {
-    position: fixed;
-    left: 0;
+    position: sticky;
     top: 0;
     width: 64px;
     height: 100vh;
@@ -149,11 +177,6 @@ body {
     }
 }
 
-/* .collapsed class: keep sidebar at icon width */
-.sidebar.collapsed {
-    width: 64px;
-}
-
 /* Sidebar Header */
 .sidebar-header {
     display: flex;
@@ -533,37 +556,82 @@ h3 {
 
 /* Navigation */
 .top-nav {
+    position: relative;
     display: flex;
-    justify-content: space-between;
+    justify-content: center;
     align-items: center;
     margin-bottom: 2rem;
-    flex-wrap: wrap;
-    gap: 1.5rem;
+    padding-top: 1rem;
+    width: 100%;
+}
+.top-nav header {
+    text-align: center;
+}
+
+/* Smart Back Button */
+.smart-back-btn {
+    position: absolute;
+    left: 1rem;
+    top: 1rem;
+    display: flex;
+    align-items: center;
+    gap: 0.5rem;
+    padding: 0.5rem 1rem;
+    background: var(--bg-white);
+    border: 1px solid var(--glass-border);
+    border-radius: 8px;
+    color: var(--text-main);
+    font-size: 0.9rem;
+    font-weight: 600;
+    cursor: pointer;
+    box-shadow: 0 4px 15px rgba(30, 58, 138, 0.05);
+    transition: all 0.3s ease;
+    z-index: 1000;
+}
+
+.smart-back-btn:hover {
+    background: linear-gradient(135deg, rgba(30, 58, 138, 0.02), rgba(30, 58, 138, 0.01));
+    transform: translateX(-3px);
+    box-shadow: 0 6px 20px rgba(30, 58, 138, 0.1);
+}
+
+.smart-back-btn.disabled {
+    opacity: 0.5;
+    pointer-events: none;
+    filter: grayscale(1);
 }
 
 /* Account Bar Styling */
 .account-bar {
-    position: relative;
-    z-index: 100;
+    position: absolute;
+    top: 1rem;
+    right: 2rem;
+    z-index: 1000;
+    padding-bottom: 1rem; /* Invisible padding bridging the gap to dropdown */
+}
+
+/* Expanded invisible hover bridge to make diagonal mouse movement forgiving */
+.account-bar::after {
+    content: '';
+    position: absolute;
+    top: -20px;
+    bottom: -20px;
+    left: -40px;
+    right: -40px;
+    z-index: -1;
 }
 
 .account-profile {
     display: flex;
     align-items: center;
-    gap: 0.6rem;
-    background: var(--bg-white);
-    border: 1px solid var(--glass-border);
-    padding: 0.4rem 1rem 0.4rem 0.4rem;
-    border-radius: 40px;
+    justify-content: center;
     cursor: pointer;
-    box-shadow: 0 4px 15px rgba(30, 58, 138, 0.08);
     transition: all 0.3s ease;
+    border-radius: 50%;
 }
 
 .account-profile:hover {
     transform: translateY(-2px);
-    box-shadow: 0 6px 20px rgba(30, 58, 138, 0.12);
-    border-color: rgba(30, 58, 138, 0.2);
 }
 
 .avatar {
@@ -585,10 +653,9 @@ h3 {
     box-shadow: 0 0 15px rgba(59, 130, 246, 0.6);
 }
 
+/* Hidden by default, keeping avatar only */
 .account-info {
-    display: flex;
-    flex-direction: column;
-    margin-right: 0.5rem;
+    display: none;
 }
 
 .user-name {
@@ -597,67 +664,13 @@ h3 {
     color: var(--text-main);
 }
 
-.account-type {
-    font-size: 0.75rem;
-    color: var(--text-muted);
-    display: flex;
-    align-items: center;
-    gap: 0.3rem;
-}
-
-.pro-badge {
-    background: linear-gradient(to right, #f59e0b, #ef4444);
-    color: white;
-    padding: 0.1rem 0.4rem;
-    border-radius: 8px;
-    font-size: 0.65rem;
-    font-weight: 700;
-}
-
-.notification-icon {
-    position: relative;
-    font-size: 1.2rem;
-    margin-left: 0.25rem;
-    margin-right: 0;
-    color: var(--text-muted);
-    transition: color 0.3s;
-    cursor: pointer;
-    padding: 0.25rem;
-    border-radius: 50%;
-    display: flex;
-    align-items: center;
-    justify-content: center;
-}
-
-/* Vertical separator between bell and avatar */
-.account-bar-divider {
-    width: 1px;
-    height: 24px;
-    background: var(--glass-border);
-    border-radius: 2px;
-    flex-shrink: 0;
-    margin: 0 0.25rem;
-}
 
-.account-profile:hover .notification-icon {
-    color: var(--primary);
-}
 
-.notification-badge {
-    position: absolute;
-    top: 0px;
-    right: -2px;
-    width: 8px;
-    height: 8px;
-    background-color: var(--danger);
-    border-radius: 50%;
-    border: 2px solid var(--bg-white);
-}
 
 /* Account Dropdown */
 .dropdown-menu {
     position: absolute;
-    top: calc(100% + 0.8rem);
+    top: 100%; /* Sits exactly at bottom of account-bar's padding */
     right: 0;
     background: var(--bg-white);
     border: 1px solid var(--glass-border);
@@ -667,17 +680,25 @@ h3 {
     overflow: hidden;
     transform-origin: top right;
     
-    /* Animation states */
+    /* Default state (Hidden, Unhovered) */
     opacity: 0;
-    transform: scale(0.95) translateY(-10px);
+    transform: scale(0.95) translateY(-5px);
     visibility: hidden;
-    transition: opacity 0.3s ease, transform 0.3s ease, visibility 0.3s;
+    pointer-events: none;
+    
+    /* 250ms Delay before hiding when mouse leaves */
+    transition: opacity 0.3s ease 0.25s, transform 0.3s ease 0.25s, visibility 0s 0.55s;
 }
 
-.dropdown-menu.active {
+/* Hover trigger for dropdown instead of click */
+.account-bar:hover .dropdown-menu {
     opacity: 1;
     transform: scale(1) translateY(0);
     visibility: visible;
+    pointer-events: auto;
+    
+    /* Opens instantly when hovered */
+    transition: opacity 0.2s ease, transform 0.2s ease, visibility 0s;
 }
 
 .dropdown-menu ul {
@@ -712,53 +733,7 @@ h3 {
     color: var(--danger);
 }
 
-/* Notifications Dropdown */
-#notif-dropdown {
-    width: 280px;
-    right: 50%;
-    transform-origin: top right;
-}
-
-.notif-header {
-    padding: 0.75rem 1rem;
-    font-weight: 600;
-    border-bottom: 1px solid var(--glass-border);
-    color: var(--text-main);
-    font-size: 0.95rem;
-}
-
-#notif-list {
-    max-height: 250px;
-    overflow-y: auto;
-    list-style: none;
-    padding: 0;
-    margin: 0;
-}
-
-#notif-list li {
-    padding: 0.75rem 1rem;
-    font-size: 0.85rem;
-    border-bottom: 1px solid rgba(30, 58, 138, 0.05);
-    color: var(--text-muted);
-    line-height: 1.4;
-    transition: background 0.2s;
-}
-
-#notif-list li:last-child {
-    border-bottom: none;
-}
-
-#notif-list li.unread {
-    color: var(--text-main);
-    background: rgba(30, 58, 138, 0.05);
-    border-left: 3px solid var(--primary);
-}
 
-#notif-list li.empty-notif {
-    text-align: center;
-    border: none;
-    padding: 1.5rem 1rem;
-}
 
 /* Forms */
 .form-grid {
@@ -1219,6 +1194,31 @@ input:focus, select:focus {
     border-bottom-right-radius: 4px;
 }
 
+/* Typing indicator bubble */
+.typing-bubble {
+    display: flex;
+    align-items: center;
+    gap: 5px;
+    padding: 0.75rem 1rem;
+}
+
+.typing-bubble .dot {
+    width: 8px;
+    height: 8px;
+    border-radius: 50%;
+    background: var(--primary);
+    opacity: 0.6;
+    animation: typing-bounce 1.2s infinite ease-in-out;
+}
+
+.typing-bubble .dot:nth-child(2) { animation-delay: 0.2s; }
+.typing-bubble .dot:nth-child(3) { animation-delay: 0.4s; }
+
+@keyframes typing-bounce {
+    0%, 80%, 100% { transform: translateY(0); opacity: 0.4; }
+    40%            { transform: translateY(-6px); opacity: 1; }
+}
+
 .chat-input-area {
     padding: 1rem;
     border-top: 1px solid var(--glass-border);
@@ -1639,9 +1639,6 @@ select.readonly-input[disabled]:focus {
     .sidebar {
         width: 56px;
     }
-    .layout-dashboard .main-content {
-        margin-left: 56px;
-    }
 }
 
 @media (max-width: 768px) {
@@ -2231,3 +2228,144 @@ select.readonly-input[disabled]:focus {
     #home-section .features-grid { grid-template-columns: 1fr; }
     .about-split-section { padding: 5rem 0; }
 }
+
+/* Auth Tabs */
+.auth-tabs {
+    display: flex;
+    justify-content: center;
+    gap: 1rem;
+    margin-bottom: 2rem;
+    border-bottom: 1px solid var(--glass-border);
+    padding-bottom: 1rem;
+}
+
+.auth-tab {
+    background: transparent;
+    border: none;
+    color: var(--text-muted);
+    font-size: 1rem;
+    font-weight: 600;
+    cursor: pointer;
+    padding: 0.5rem 1rem;
+    transition: all var(--transition-fast);
+}
+
+.auth-tab:hover {
+    color: var(--primary);
+}
+
+.auth-tab.active {
+    color: var(--primary);
+    border-bottom: 2px solid var(--primary);
+}
+
+/* Organization Sub-tabs */
+.org-sub-tabs {
+    display: flex;
+    gap: 0.5rem;
+    margin-bottom: 2rem;
+    overflow-x: auto;
+    padding-bottom: 0.5rem;
+}
+
+.org-sub-tab {
+    background: rgba(255, 255, 255, 0.05);
+    border: 1px solid var(--glass-border);
+    color: var(--text-muted);
+    padding: 0.6rem 1.2rem;
+    border-radius: 8px;
+    font-size: 0.9rem;
+    font-weight: 500;
+    cursor: pointer;
+    white-space: nowrap;
+    transition: all var(--transition-fast);
+}
+
+.org-sub-tab:hover {
+    background: rgba(255, 255, 255, 0.1);
+    color: var(--text-primary);
+}
+
+.org-sub-tab.active {
+    background: var(--primary);
+    color: white;
+    border-color: var(--primary);
+    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
+}
+
+/* Health Badge & Circle */
+.health-badge {
+    background: var(--primary);
+    color: white;
+    padding: 0.3rem 0.8rem;
+    border-radius: 20px;
+    font-size: 0.8rem;
+    font-weight: 700;
+}
+
+.health-circle {
+    width: 120px;
+    height: 120px;
+    border-radius: 50%;
+    border: 8px solid var(--primary);
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    font-size: 2.2rem;
+    font-weight: 800;
+    color: var(--text-primary);
+    background: var(--glass-bg);
+    box-shadow: 0 0 20px rgba(37, 99, 235, 0.2);
+    margin-bottom: 1rem;
+}
+
+/* Advisory & Anomaly Cards */
+.advisory-grid {
+    display: grid;
+    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
+    gap: 1.5rem;
+    margin-top: 1rem;
+}
+
+.advisory-card {
+    padding: 1.5rem;
+    background: rgba(255, 255, 255, 0.03);
+    border-left: 4px solid var(--primary);
+    border-radius: 8px;
+}
+
+.advisory-card.urgent { border-left-color: var(--danger); }
+.advisory-card.strategic { border-left-color: var(--primary); }
+.advisory-card.growth { border-left-color: var(--success); }
+
+.anomaly-item {
+    padding: 1rem;
+    background: rgba(239, 68, 68, 0.05);
+    border-radius: 8px;
+    margin-bottom: 0.8rem;
+    border-left: 3px solid var(--danger);
+}
+
+.stat-value {
+    font-size: 1.4rem;
+    font-weight: 700;
+    display: block;
+    margin-top: 0.3rem;
+}
+
+.stat-row {
+    display: flex;
+    justify-content: space-between;
+    padding: 0.8rem 0;
+    border-bottom: 1px solid rgba(255,255,255,0.05);
+}
+
+.badge-ai {
+    background: linear-gradient(135deg, #8b5cf6, #3b82f6);
+    color: white;
+    padding: 0.2rem 0.6rem;
+    border-radius: 4px;
+    font-size: 0.7rem;
+    text-transform: uppercase;
+    letter-spacing: 0.5px;
+}
